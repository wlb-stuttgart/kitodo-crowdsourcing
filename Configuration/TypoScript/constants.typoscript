plugin.tx_crowdsourcing {

    settings {
        # cat=plugin.tx_crowdsourcing//a; type=integer; label=PID of the crowdsourcing frontend user group
        crowdsourcingUserGroup =

        # cat=plugin.tx_crowdsourcing//a; type=integer; label=Register PID
        crowdsourcingRegister =

        # cat=plugin.tx_crowdsourcing//a; type=integer; label=Login PID
        crowdsourcingLogin =
    }
}

